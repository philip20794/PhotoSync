# Prisma

Platz für Schema und Migrationen. Noch kein Client oder Schema implementiert. Prisma ORM wird ausschließlich gegen das lokale PostgreSQL verwendet; keine Prisma-Cloud-Dienste. Version und PostgreSQL-Treiber beim ersten Schema gemeinsam fixieren. Siehe `../../docs/data-model.md`.
